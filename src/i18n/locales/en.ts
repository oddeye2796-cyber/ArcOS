/**
 * Everything the app needs for one language, in a single chunk.
 *
 * The bundler names the chunk after this file, so a build report shows which
 * language each one carries.
 */
export { default as translations } from './translations.en';
export { default as catalog } from './catalog.en';
